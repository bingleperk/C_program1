#include <stdio.h>
#include "hw_2_input.h"

void minmax (float A[64][64], float *min, float *max);


int main()
{
    float A[64][64]; //matrix A
    float min=A[0][0], max=A[0][0]; //값 초기화
    minmax(A, &min, &max); //최댓값 

    float scale;

    

    return 0;
}





void minmax (float A[64][64], float *min, float *max) //최소 최대 뜯어오기.
{
        int i, j;
        for (i=0; i<64; i++)
        {
            for(j=0; j<64; j++)
            {
                *min=(*min>A[i][j]) ? A [i][j] : *min;
                *max=(*max<A[i][j]) ? A [i][j] : *max;
            }
        }

}
